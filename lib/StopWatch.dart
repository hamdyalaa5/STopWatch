import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Stopwatch extends StatelessWidget {
 static const String routName = "Stopwatch";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      

    );
  }
}